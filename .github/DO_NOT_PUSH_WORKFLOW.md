GitHub blocks pushing workflow files unless your token has `workflow` scope.
If you need CI workflow enabled, push `.github/workflows/ci.yml` from a machine with a PAT that includes `workflow` scope.
